.type-mini {
  border-radius: 40rpx;
  padding: 40rpx 28rpx;
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  color: #1A1A2E;
  position: relative;
  overflow: hidden;
}

.loading-dots {
  display: flex;
  gap: 8rpx;
}

.loading-dots view {
  width: 14rpx;
  height: 14rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #2D3436, #636E72);
  animation: bounce 0.9s ease infinite;
}

.loading-dots view:nth-child(2) { animation-delay: 0.15s; }
.loading-dots view:nth-child(3) { animation-delay: 0.3s; }

@keyframes bounce {
  0%, 100% { transform: translateY(0); opacity: 0.4; }
  50% { transform: translateY(-6px); opacity: 1; }
}

.skeleton {
  border-radius: 40rpx;
  background: linear-gradient(90deg, rgba(0,0,0,0.04), #F8FAFB, rgba(0,0,0,0.04));
  background-size: 200% 100%;
  animation: shimmer 1.5s infinite;
}

.shot-panel {
  margin-top: 24rpx;
  padding: 24rpx;
  border-radius: 28rpx;
  background: #F8FAFB;
}

.shot-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 14rpx;
}

.shot-tile {
  position: relative;
  height: 128rpx;
  border-radius: 22rpx;
  overflow: hidden;
  background: #EDF4FA;
}

.shot-img {
  width: 100%;
  height: 100%;
  display: block;
}

.shot-remove {
  position: absolute;
  top: 8rpx;
  right: 8rpx;
  width: 34rpx;
  height: 34rpx;
  border-radius: 50%;
  background: rgba(26,26,46,0.72);
  color: #fff;
  font-size: 26rpx;
  line-height: 34rpx;
  text-align: center;
  font-weight: 800;
}

.shot-order {
  position: absolute;
  left: 8rpx;
  bottom: 8rpx;
  min-width: 30rpx;
  height: 30rpx;
  padding: 0 8rpx;
  border-radius: 999rpx;
  background: rgba(255,255,255,0.86);
  color: #2D3436;
  font-size: 20rpx;
  line-height: 30rpx;
  text-align: center;
  font-weight: 800;
}

.shot-size {
  position: absolute;
  right: 8rpx;
  bottom: 8rpx;
  max-width: 76rpx;
  height: 30rpx;
  padding: 0 8rpx;
  border-radius: 999rpx;
  background: rgba(255,255,255,0.86);
  color: #8395A7;
  font-size: 18rpx;
  line-height: 30rpx;
  text-align: center;
  font-weight: 800;
  overflow: hidden;
}

.profile-sync {
  display: flex;
  align-items: center;
  gap: 20rpx;
  margin-top: 16rpx;
  padding: 28rpx 30rpx;
  border-radius: 28rpx;
  background: #FFFFFF;
  box-shadow: 0 8rpx 34rpx rgba(0,0,0,0.04);
}

.profile-sync-title {
  color: #1A1A2E;
  font-size: 27rpx;
  font-weight: 900;
  margin-bottom: 6rpx;
}

.profile-sync-desc {
  color: #8395A7;
  font-size: 23rpx;
  line-height: 1.5;
}

.profile-sync-btn {
  flex-shrink: 0;
  min-width: 118rpx;
  height: 64rpx;
  margin: 0;
  padding: 0 22rpx;
  border-radius: 999rpx;
  background: #2D3436;
  color: #FFFFFF;
  font-size: 24rpx;
  font-weight: 900;
  line-height: 64rpx;
}

.profile-sync-btn.done {
  background: #F0FFF4;
  color: #00B894;
}

.profile-sync-btn::after {
  border: none;
}
